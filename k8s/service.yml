apiVersion: v1
kind: Service
metadata:
  name: komer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "komer"
    port: 8080
  selector:
    app: komer
