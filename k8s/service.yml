apiVersion: v1
kind: Service
metadata:
  name: maker
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "maker"
    port: 8080
  selector:
    app: maker
