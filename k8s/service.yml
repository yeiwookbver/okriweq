apiVersion: v1
kind: Service
metadata:
  name: foerm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "foerm"
    port: 8080
  selector:
    app: foerm
