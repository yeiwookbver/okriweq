apiVersion: v1
kind: Service
metadata:
  name: comker
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "comker"
    port: 8080
  selector:
    app: comker
