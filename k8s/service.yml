apiVersion: v1
kind: Service
metadata:
  name: booker
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "booker"
    port: 8080
  selector:
    app: booker
