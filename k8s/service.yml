apiVersion: v1
kind: Service
metadata:
  name: goerm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "goerm"
    port: 8080
  selector:
    app: goerm
