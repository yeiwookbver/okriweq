apiVersion: v1
kind: Service
metadata:
  name: xroke
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xroke"
    port: 8080
  selector:
    app: xroke
