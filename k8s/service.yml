apiVersion: v1
kind: Service
metadata:
  name: byoerk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "byoerk"
    port: 8080
  selector:
    app: byoerk
