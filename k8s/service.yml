apiVersion: v1
kind: Service
metadata:
  name: comkek
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "comkek"
    port: 8080
  selector:
    app: comkek
