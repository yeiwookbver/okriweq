apiVersion: v1
kind: Service
metadata:
  name: xoskewe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xoskewe"
    port: 8080
  selector:
    app: xoskewe
